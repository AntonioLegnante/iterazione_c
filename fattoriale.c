/* fattoriale */

int main ()
{
    int 
    
    for(i = 1; i <= 5; i++)
    {
        temp = i;

        if (i - 1 != 0)
        {
            while(fattoriale - 1 != 0)
            {
                decremento = i--;
                fattoriale *= decremento;
            }
        
        else 
            fattoriale = i;
        
            i = temp;

            printf("%d", fattoriale);
  
     }
  
        return 0;

}